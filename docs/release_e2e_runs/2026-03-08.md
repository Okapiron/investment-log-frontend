# TradeTrace 公開前E2E実施記録

- 実施日: 2026-03-08
- 実施者: Codex + hiroki
- 対象環境: Production
- Backend URL: `https://tradetrace-api.onrender.com`
- Frontend URL: `https://investment-log-frontend.vercel.app`
- Backend Version: `2026.03.08`
- Frontend Build: `index-X6uzT-Dg.js`（本番配信HTMLから確認）

## 1. 自動確認結果

### 1.1 Backend preflight（招待制公開基準）
- コマンド:
  - `cd backend && .venv/bin/python tools/preflight_release.py --base https://tradetrace-api.onrender.com --expect-auth-required --json`
- 結果: `FAIL`
- 主な理由:
  - `settings/me requires auth` が `status=200`
  - `settings/runtime requires auth` が `status=200`
  - `trades requires auth` が `status=200`

### 1.2 Backend preflight（現行運用基準: auth-off）
- コマンド:
  - `cd backend && .venv/bin/python tools/preflight_release.py --base https://tradetrace-api.onrender.com --no-expect-auth-required --json`
- 結果: `PASS`

### 1.3 API生存（ケースE）
- `GET /health`: `PASS`
- `GET /health/ready`: `PASS`
- `GET /openapi.json`: preflight内で `PASS`

### 1.4 Frontend直アクセス
- `GET /auth`: `200`
- `GET /trades`: `200`
- 直アクセス404は再発していない

## 2. Runtime確認
- `GET /api/v1/settings/runtime`:
  - `auth_enabled=false`
  - `invite_code_required=false`
  - `release_status=ok`
  - `config_errors=[]`
  - `config_warnings=[]`

## 3. 手動確認（未実施）
- ケースA（新規招待ユーザー導線）: `PENDING`
- ケースB（招待コード1回利用制限）: `PENDING`
- ケースC（ユーザー分離）: `PENDING`
- ケースD（Settings詳細操作）: `PENDING`

## 4. 判定
- 招待制公開（Phase 1）基準: `NO-GO`
  - 理由: 本番設定が `AUTH OFF / INVITE OFF` のため、招待制受け入れ条件を満たさない
- 現行ベータ（auth-off運用）基準: `GO`
  - 理由: 現行モードの自動preflightは通過

## 5. 次アクション
1. Render環境変数で `AUTH_ENABLED=true` と `INVITE_CODE_REQUIRED=true` を有効化
2. 有効な招待コードを最低1件発行
3. 本ドキュメントと同じ手順で preflight を `--expect-auth-required` で再実行
4. ケースA〜Dの手動確認を実施して `GO` 判定に更新

## 6. 観測メモ（軽微）
- Frontend HTMLの `<title>` はまだ `Asset Management MVP` になっている
- プロダクト表示名に合わせるなら別タスクで `Trade Trace` に統一する
